import React from 'react';
import Spinners from '../img/spinner.gif';
const Spinner = () => {
  return (
    <img
      src={Spinners}
      style={{width: '200px', margin: 'auto', display: 'block'}}
    />
  );
};

export default Spinner;
